using AutoMapper;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Kodigos.API.IntegracaoProtheus.Mappers
{
    public class UsuarioMapper
    {
        /*public static List<UsuarioDTO> GetUsuarioERP(UsuariosAPI _usuario)
        {
            var config = new MapperConfiguration(cfg =>
            {
                cfg.CreateMap<UsuarioAPI, UsuarioDTO>();
            });

            IMapper iMapper = config.CreateMapper();

            var list = new List<UsuarioDTO>();

            foreach(var aux in _usuario.Usuarios)
            {
                var destination = iMapper.Map<UsuarioAPI, UsuarioDTO>(aux);
                list.Add(destination);
            }
            
            return list;

        }*/


    }
}
